# Environment Configuration
PORT=3000
NODE_ENV=development
JWT_SECRET=
SESSION_SECRET=

# Database
DATABASE_URL=./database/portfolio.sqlite

# Optional SMTP for live email delivery (if unconfigured, contact messages are stored securely in SQLite)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASSWORD=
CONTACT_EMAIL=chukwuemekavictor726@gmail.com

# Optional GitHub Integration
GITHUB_TOKEN=
